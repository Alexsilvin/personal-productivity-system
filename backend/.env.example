# Local development environment
NODE_ENV=development
PORT=3000

# PostgreSQL connection string for the local Docker database
# docker compose up -d from the repository root starts this database on localhost:5432
DATABASE_URL=postgresql://pps:pps@localhost:5432/pps
